// Drawflow ESM shim
// Loads the UMD bundle as a classic script, then exports it as a default export.
// Must be loaded with type="module" AFTER the classic script tag loads Drawflow.

export default window.Drawflow;
