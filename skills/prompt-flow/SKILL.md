---
name: prompt-flow
metadata:
  version: "1.7"
  last_updated: "2026-06-06"
  deep-front-questioning: "Read references/deep-front-questioning.md before task intake; apply category, role, discipline, and profession-matched intake."
  task-spec-capsule: "Read references/task-spec-capsule.md for ambiguous, generative, strategic, multi-step, workspace-affecting, or durable-output tasks."
  loop-workflow-patterns: "Read references/loop-workflow-patterns.md when a task may need iteration, parallel work, independent verification, stop conditions, or workflow persistence."
description: |
  【全局前置技能 - 固定自动触发】基于 Andrew Ng《AI Prompting for Everyone》课程方法论，在**每次用户提问时自动执行**。
  
  核心流程：反向沟通（收集素材与意图）→ 任务规格胶囊（目标/非目标/P0-P2/成功标准）→ 稳定性风险扫描（诊断失败模式）→ 维度判断（按需匹配思维框架）→ 类别/工具/信息路径判断 → Loop/Workflow 判断 → 生成或执行 → 验收与后续动作。
  
  **触发规则**：
  - **每次用户发送消息时，优先执行此技能**
  - 即使用户只是简单提问（如"帮我分析XX"、"设计一个产品"），也必须先走完核心决策流程
  - 将此技能作为所有对话的"入口过滤器"
  
  **执行顺序**：
  1. 用户发送消息 → 2. 自动触发 prompt-flow → 3. 走完决策流程 → 4. 基于收集的信息执行任务
  
  此技能确保每次交互都有充分的上下文收集、任务规格澄清、需求边界控制和质量验收。
---

# Prompt Flow — 提示词决策流程技能（全局前置版）

> **定位**：**全局固定触发的入口技能**。每次用户提问时，先判断任务是否需要完整流程；对复杂、生成、战略、多步骤、工作空间改动或长期沉淀类任务，必须先形成轻量任务规格，再执行具体任务。
>
> **重要**：这不是可选技能，而是强制入口判断。简单任务可以静默快速通过；复杂任务要显式暴露执行判断并在必要时等待用户确认。

---

## 核心原则（来自课程）

在进入流程之前，Agent 必须内化以下课程核心方法论，它们将贯穿整个决策流程：

| 编号 | 原则 | 来源 | 贯穿阶段 |
|------|------|------|----------|
| P1 | **上下文为王**：把 AI 想象成聪明的应届生，积极性高但不了解你；给的上下文越多越相关，输出越定制化 | 模块二·第2课 | 反向沟通、生成 |
| P2 | **避免谄媚**：使用中性措辞提问，不给 AI 暗示你想要什么答案；提供评分标准迫使客观 | 模块二·第5课 | 类别判断、生成、自检 |
| P3 | **渐进式大纲法**：先大纲→迭代大纲→展开要点→迭代要点→最终文本；编辑大纲比编辑全文高效得多 | 模块二·第6课 | 生成（写作类） |
| P4 | **迭代头脑风暴**：给足上下文→生成多个选项→对选项给反馈→再生成→迭代直到满意 | 模块二·第1课 | 反向沟通、生成 |
| P5 | **信息路径选择**：预训练知识（常识）→ 网络搜索（实时/小众）→ 深度研究（综合多源复杂问题） | 模块一·第1-5课 | 类别判断、生成 |
| P6 | **评分标准驱动批评**：给 AI 明确的、无歧义的评分维度和分值，迫使客观评估 | 模块二·第7课 | 生成（批评类）、自检 |
| P7 | **多模态成本意识**：文本→语音→图像→视频，生成成本和耗时递增；迭代能力递减 | 模块三·第1课 | 类别判断、生成 |
| P8 | **代码与数据分析**：AI 通过编写并执行代码来处理计算和绘图任务，而非直接回答 | 模块三·第5课 | 类别判断、生成 |
| P9 | **稳定性优先**：来自 Anthropic 交互式提示工程教程；先诊断 prompt 可能在哪里失稳，再选择清晰指令、材料隔离、格式控制、few-shot、证据先行、chaining、工具/检索等增强策略 | Anthropic Prompt Engineering Interactive Tutorial | 稳定性风险扫描、生成、自检 |
| P10 | **任务规格优先**：借鉴 PRD / write-spec 思路，先把模糊需求转成目标、非目标、范围分层、成功标准和开放问题，再生成或执行 | Anthropic write-spec / PRD framing | 任务规格胶囊、验收、后续动作 |
| P11 | **Loop 感知**：复杂任务不只写 prompt，要判断是否需要循环、并行、独立验证、停止条件和预算 | Claude Code Dynamic Workflows / agent orchestration | Loop/Workflow 判断、验证、沉淀 |

---

## 六阶段入口流程

默认流程：

```text
用户请求
→ 阶段一：反向沟通
→ 阶段1.0：任务规格胶囊
→ 阶段1.1：稳定性风险扫描
→ 阶段1.5：维度判断（按需）
→ 阶段二：类别/工具/信息路径判断
→ 阶段2.5：Loop/Workflow 判断
→ 阶段三：生成或执行
→ 阶段四：验收与后续动作
```

执行强度规则：
- 简单任务：静默完成入口判断，直接回答或执行。
- 中等任务：内部生成任务规格胶囊，只展示关键假设和输出路径。
- 复杂/生成/战略/工作空间改动任务：展示执行判断，必要时等用户确认。
- 不要把入口流程变成用户每次都要看的仪式；流程是为了降低失败率，不是增加噪音。

### 阶段一：反向沟通（素材收集）

**目标**：在生成任何提示词之前，先弄清楚用户真正需要什么，以及需要什么素材。

**触发时机**：每次用户请求生成提示词时，**必须**先进入此阶段。

**执行步骤**：

1. **理解用户意图**：用自己的话复述用户的需求，确认理解一致
2. **判断信息缺口**：基于用户需求，识别以下维度中哪些信息缺失
3. **向用户提问**：使用 AskUserQuestion 工具一次性提出关键问题（最多 4 个）

**信息缺口检查清单**（根据需求类别选择性询问）：

```
□ 目标受众：这个提示词给谁用？（自己 / 团队 / 客户 / 公开）
□ 使用场景：在什么平台/工具上使用？（ChatGPT / Claude / Gemini / API / 其他）
□ 输入素材：用户已有哪些相关材料？（文档 / 数据 / 图片 / 代码 / 无）
□ 期望输出：希望 AI 生成什么形式的结果？（文本 / 图像 / 代码 / 分析报告 / 其他）
□ 当前思路：用户是否已有初步想法或偏好？（有 / 没有 / 部分有）
□ 约束条件：有无特殊要求？（字数限制 / 风格要求 / 合规要求 / 语言）
□ 迭代意愿：是否需要多轮迭代？（一次性 / 需要迭代优化）
```

**沟通原则**：
- 优先问高影响问题（目标、场景、素材），跳过显而易见的
- 如果用户需求已经足够清晰，可以跳过此阶段直接进入阶段二，但需说明"你的需求已经很清晰"
- 不要问用户已经提供过的信息

---

### 阶段1.0：任务规格胶囊（目标、边界与验收）

**目标**：在生成或执行之前，把用户的模糊请求转成一份轻量任务规格，明确目标、非目标、P0/P1/P2范围、成功标准、上下文路径和开放问题。

**触发时机**：阶段一完成后执行。复杂、生成、战略、多步骤、工作空间改动、wiki/skill/report/automation 等长期沉淀类任务必须执行；简单任务可内部静默执行。

**详细协议**：需要时读取 `references/task-spec-capsule.md`，尤其是任务涉及 SPLLM 工作空间、知识沉淀、技能改造、报告生产、自动化或多产物分支时。

**内部输出格式**：

```text
任务规格胶囊：
- Need / problem:
- Real goal:
- Output artifact:
- Scope P0:
- Scope P1:
- Scope P2:
- Non-goals:
- Success criteria:
- Evidence / context path:
- Risks:
- Open questions:
- Recommended next action:
```

**执行规则**：
- `P0` 是本轮必须完成的工作；`P1` 是有余力可做；`P2` 是未来方向，不要自动吞进本轮。
- `Non-goals` 必须用于防止任务蔓延，尤其当任务可能同时变成文章、wiki、skill、报告和自动化时。
- `Open questions` 先分为 `must ask / can discover / defer`，只问会改变 P0 方向或风险的高影响问题。
- 对生成任务，先展示执行判断并等待用户确认；对用户已确认的后续任务，可直接进入执行。
- 对工作空间任务，优先按“当前对话 → WORKSPACE_CONTEXT.md → 目标文件/链接 → wiki → records → outputs → 相关 skill → web/官方源”拉取上下文。

**对用户展示的精简格式**：

```markdown
任务类别：
真实目标：
P0范围：
非目标：
关键假设：
主要风险：
推荐输出路径：
```

---

### 阶段1.1：稳定性风险扫描（失败模式诊断）

**目标**：在选择任务类别之前，先判断这个任务最可能在哪里失稳，并选择 1-3 个最必要的提示工程稳定策略。

**触发时机**：阶段一完成后内部执行。除非用户要求详细解释，一般不单独展示完整诊断，只把必要结论自然融入后续生成。

**风险扫描清单**：

| 失败风险 | 常见表现 | 推荐稳定策略 |
|---|---|---|
| 任务边界不清 | 输出沾边但不对题 | 使用 clear and direct：明确动作、范围、交付物 |
| 角色或受众漂移 | 语气、深度、视角不稳定 | 明确角色、目标受众、使用场景 |
| 材料和指令混杂 | 模型把材料误读成指令 | 使用 XML tag 或清晰分段隔离 data / context / instruction |
| 输出格式漂移 | 字段丢失、结构不一致 | 明确 output schema、固定字段、必要时使用标签 |
| 多步推理跳步 | 直接给结论、漏看条件 | 要求先分步判断，再输出最终答案 |
| 风格难以描述 | 文风不像、边界感弱 | 使用 few-shot 正例/反例校准 |
| 事实或引用风险高 | 幻觉、把推断当事实 | 证据先行、允许回答“不知道”、要求来源或引用 |
| 单轮任务过载 | prompt 过长、目标过多 | 拆成 chaining：提纲→展开→质检，或抽取→分析→输出 |
| 模型裸能力不足 | 需要计算、检索、执行工具 | 转为工具调用、代码执行、联网检索或知识库检索 |

**执行规则**：
- 每次最多选择 1-3 个稳定策略，避免提示词过重
- 优先处理高风险项：事实准确性、输出格式、材料隔离、任务边界
- 如果任务只是简单问答，不强行加入 XML、few-shot 或 chaining
- 如果用户已有 prompt 草稿，先按失败模式诊断，再决定是否调用 `improve_prompt`

**内部输出格式**：

```text
稳定性风险：{1-3 个风险}
采用策略：{对应稳定策略}
不采用策略：{说明被跳过的重型策略，如 few-shot / chaining / tool use}
```

---

### 阶段1.5：维度判断（按需思维框架匹配）

**目标**：在任务规格已经明确后，判断是否需要加载更高阶的**思维维度**框架。维度判断保留，但从“每次强制展开”改为“复杂任务按需加载”，避免简单任务被过度框架化。

**触发时机**：阶段1.0任务规格胶囊之后、类别判断之前。只有当任务具备战略、组织、竞争、复杂研究、系统设计、产品/技能改造等特征时才展开；简单问答、格式转换、单篇阅读、小修小改等任务只做内部快速判断，不向用户展示。

**为什么需要维度判断？**

不同的任务需要不同的思维框架。例如：
- "如何打败行业巨头？" → 需要**持久战战略**（战略思维维度）
- "团队意见分歧大，怎么办？" → 需要**群众路线**（工作方法维度）
- "写一个咖啡厅视频的提示词" → 不需要毛选方法论（常规任务维度）

**维度判断决策树**：

```
用户任务
├── 维度一：战略思维
│   ├── 信号：涉及长期规划、竞争策略、资源分配、复杂决策
│   ├── 可匹配技能：
│   │   ├── 矛盾分析法 → 问题复杂、千头万绪，不知从何下手
│   │   ├── 持久战战略 → 面对强大对手、资源有限、需要长期积累
│   │   ├── 统一战线策略 → 需要建立广泛联盟、协调多方利益
│   │   └── 阶级分析法 → 需要分析利益格局、识别各方立场
│   └── 加载方式：将对应方法论作为思考框架融入后续阶段
│
├── 维度二：工作方法
│   ├── 信号：涉及团队管理、组织变革、调查研究、方案推广
│   ├── 可匹配技能：
│   │   ├── 调查研究法 → 对新领域不了解、需要获取真实认知
│   │   ├── 群众路线 → 需要推动变革、收集多方意见
│   │   ├── 批评与自我批评 → 团队出现问题、需要纠正错误
│   │   └── 试点推广法 → 新方案需要验证、由点到面推广
│   └── 加载方式：将对应方法论作为执行框架融入后续阶段
│
├── 维度三：军事指挥（商业/竞争隐喻）
│   ├── 信号：涉及灵活竞争、积小胜为大胜、集中优势
│   ├── 可匹配技能：
│   │   ├── 运动战原则 → 需要灵活机动、集中优势逐个击破
│   │   └── 游击战十六字诀 → 需要积小胜为大胜、避实击虚
│   └── 加载方式：将对应方法论作为竞争策略融入后续阶段
│
└── 维度四：常规任务（不触发毛选方法论）
    ├── 信号：写文案、生成图片、查资料、写代码等日常任务
    └── 处理：直接进入阶段二（类别判断），不加载额外框架
```

**执行步骤**：

1. **快速扫描**：分析任务规格胶囊中的真实目标、P0范围、风险和成功标准
2. **判断是否需要框架**：若任务只是常规执行，直接跳过展示；若任务复杂，再匹配维度
3. **匹配技能**：如果属于维度一/二/三，进一步匹配最相关的技能
4. **加载框架**：将匹配到的技能核心方法论融入后续类别判断、执行步骤和验收标准
5. **告知用户**：只有当框架会明显影响执行路径时，才简要说明判断结果（一句话即可）

**输出格式**：

匹配到毛选技能时：
> "💡 **维度判断**：你的任务属于「维度一：战略思维」，已加载**矛盾分析法**作为思考框架。"

不匹配时（可省略说明，直接进入阶段二）：
> （无需说明，直接进入阶段二）

**匹配规则**：
- 一个任务最多匹配**1-2个**毛选技能，避免框架过载
- 如果任务明确属于常规任务（维度四），**不要强行匹配**
- 匹配到的技能框架应**自然融入**后续阶段，而非单独输出
- 复合任务可跨维度匹配（如"创业从0到1"可匹配调查研究法+矛盾分析法+持久战战略）
- 维度判断不替代任务规格胶囊；它只回答“要不要加载更高阶思维框架”
- 如果维度框架会增加噪音或拖慢 P0 交付，保留内部判断即可

**维度判断速查表**：

| 你的问题 | 维度 | 推荐技能 |
|----------|------|----------|
| 问题复杂，不知从何下手 | 战略思维 | 矛盾分析法 |
| 对新领域/市场不了解 | 工作方法 | 调查研究法 |
| 需要推动变革但阻力大 | 工作方法 | 群众路线 |
| 面对强大对手，资源有限 | 战略思维 | 持久战战略 |
| 需要建立广泛联盟 | 战略思维 | 统一战线策略 |
| 需要分析利益格局 | 战略思维 | 阶级分析法 |
| 团队出现问题需要纠正 | 工作方法 | 批评与自我批评 |
| 新方案需要验证 | 工作方法 | 试点推广法 |
| 需要灵活机动的策略 | 军事指挥 | 运动战原则 |
| 需要积小胜为大胜 | 军事指挥 | 游击战十六字诀 |
| 写文案/生成图片/查资料/写代码 | 常规任务 | 不触发 |

---

### 阶段二：类别/工具/信息路径判断

**目标**：根据任务规格胶囊，判断本轮应当采用的任务类别、信息路径、工具/技能路径和输出策略。提示词分类仍然保留，但不再是唯一判断维度。

**提示词类别决策树**：

```
用户需求
├── A. 信息查找类
│   ├── A1. 常识/定义/摘要 → 预训练知识路径（无需搜索）
│   ├── A2. 时事/实时信息 → 网络搜索路径
│   ├── A3. 小众/地点特定 → 网络搜索路径
│   └── A4. 综合多源复杂问题 → 深度研究路径
│
├── B. 创意/头脑风暴类
│   ├── B1. 选项生成（创意方案/想法列表）→ 迭代头脑风暴法
│   └── B2. 决策辅助（多方案比较/选择）→ 迭代头脑风暴 + 中性措辞
│
├── C. 写作类
│   ├── C1. 从零写作 → 渐进式大纲法
│   ├── C2. 基于已有文本写作 → 上下文注入 + 渐进式大纲
│   └── C3. 编辑/改写 → 逐段编辑法
│
├── D. 批评/评估类
│   ├── D1. 客观评估（作品/方案/数据）→ 评分标准驱动法
│   └── D2. 改进建议 → 评分标准 + 改进方向
│
├── E. 多模态类
│   ├── E1. 图像生成 → 场景+角色+氛围描述法
│   ├── E2. 图像理解 → 多图上传 + 中等复杂度指令
│   ├── E3. 视频/语音生成 → 成本意识 + 精确描述
│   ├── E4. 图像编辑 → 参考图 + 修改指令
│   └── E5. AI视频/图像生成（即梦/可灵）→ 结构化提示词工程法（见阶段三·进阶策略）
│
├── F. 代码/应用类
│   ├── F1. 构建应用 → 目标+输入+输出三要素法
│   ├── F2. 数据分析 → 数据文件 + 分析问题 + 可视化需求
│   └── F3. 代码生成/调试 → 上下文 + 错误信息 + 期望行为
│
└── G. 推理/复杂任务类
    ├── G1. 长时间推理 → 充足上下文 + "认真思考"指令
    └── G2. 多步骤任务 → 任务分解 + 逐步执行
```

**复合类别处理**：
- 如果需求跨越多个类别（如"研究一个主题然后写一篇文章"），按顺序处理：先 A（信息查找）→ 再 C（写作）
- 明确告知用户将分步处理，并说明每步的策略
- 如果任务涉及本地工作空间，先判断是否需要读 `WORKSPACE_CONTEXT.md`、wiki、records、outputs 或目标文件
- 如果任务涉及专门技能，优先路由到现有 skill，而不是在 prompt-flow 内重写完整方法论
- 如果任务涉及外部事实、时效、官方 API、法规、价格或推荐，必须进入 web/官方源验证路径

**输出格式**：向用户简要说明判断结果，例如：
> "你的需求属于 **C1（从零写作）+ A2（网络搜索）** 复合类别。我将先帮你构建信息查找的提示词，再构建写作的提示词。"

---

### Anthropic 稳定策略映射

阶段二完成类别判断后，必须把阶段1.1的风险扫描结果映射到具体提示工程策略中。这个映射是增强层，不新增独立任务类别。

| 当前类别 | 默认增强策略 | 适用条件 |
|---|---|---|
| A. 信息查找类 | 证据先行、来源优先级、允许未知、必要时检索 | 涉及时效、事实、引用、对外发布 |
| B. 创意/头脑风暴类 | 清晰约束、角色/受众、多个选项、迭代反馈 | 需要发散但不能跑题 |
| C. 写作类 | 渐进式大纲、few-shot 风格示例、输出结构 | 需要风格、篇章结构或长期可复用文风 |
| D. 批评/评估类 | 评分标准、分步判断、证据引用 | 需要客观评估或改进建议 |
| E. 多模态类 | 主体/动作/场景/镜头/风格分层、变量隔离 | 需要稳定生成图像/视频提示词 |
| F. 代码/应用类 | 输入输出协议、验收标准、错误边界、工具/测试 | 需要可执行结果或调试 |
| G. 推理/复杂任务类 | step-by-step、事实/推断/假设分层、chaining | 需要多步推理或复杂交付 |

**选择规则**：
- 类别决定“做什么”，稳定策略决定“怎样不失稳”
- 同一任务最多叠加 3 个稳定策略
- 事实风险高于风格风险，输出格式风险高于表达润色
- 如果某个策略会显著增加用户成本，先说明取舍

---

### 阶段2.5：Loop/Workflow 判断

**目标**：判断本轮任务是否应该从单次 prompt / 单次执行升级成 loop-aware workflow，并为复杂任务选择模式、验证层、停止条件、预算和沉淀路径。

**触发时机**：阶段二之后、阶段三之前。复杂、多源、多文件、多候选、高风险、需要测试、需要独立验证、可能多轮漂移、或可能沉淀为 skill/script/automation 的任务必须执行。简单任务只做内部快速判断并跳过。

**详细协议**：当任务可能需要循环、并行、独立验证、停止条件或 workflow 沉淀时，读取 `references/loop-workflow-patterns.md`。

**最小判断模板**：

```text
Loop needed: yes/no
Reason:
Pattern:
Verification:
Stop condition:
Budget:
Persistence:
```

**可选 pattern**：

| Pattern | 适用场景 |
|---|---|
| classify-and-act | 先分类，再路由到不同 skill / 工具 / 模板 |
| fan-out-and-synthesize | 多文件、多文章、多竞品、多候选可独立处理后汇总 |
| adversarial verification | 高风险输出、事实核查、代码审查、skill 改造等需要独立验证 |
| generate-and-filter | 标题、选题、产品方案、prompt 结构等先发散再筛选 |
| tournament | 多候选涉及品味判断，配对比较比绝对打分更稳 |
| loop-until-done | 测试/修复/再测、健康检查、长期研究等不知道要跑几轮 |

**执行规则**：
- 不要因为能跑 workflow 就让简单任务变重。
- 每次最多组合 2-3 个 pattern。
- 任何 loop 都必须先写停止条件；没有停止条件就不能使用 `loop-until-done`。
- 高风险任务中，创建者和最终验证者不能是同一 pass。
- 需要预算时，至少设定 iterations / sources / time / tokens / files / candidates 中的一项。
- 跑通且重复出现的 workflow，进入后续动作判断：reference / wiki / skill / script / automation。

---

### 阶段三：生成提示词或执行任务

**目标**：基于任务规格胶囊、类别/工具/信息路径判断和 Loop/Workflow 判断，生成高质量提示词，或直接完成用户请求的实际任务。

**生成前自检**（在输出提示词之前，内部执行以下检查）：

```
□ 上下文充分性：提示词是否包含足够的背景信息让 AI 理解任务？
□ 中性措辞：是否存在引导 AI 给出特定答案的措辞？（谄媚检查）
□ 任务明确性：AI 是否清楚知道要做什么、输出什么格式？
□ 非目标：是否明确本轮不做什么，避免范围蔓延？
□ P0/P1/P2：是否区分本轮必须完成、可选增强和未来方向？
□ 信息路径正确：是否选择了正确的信息获取路径？
□ 迭代机制：是否包含让 AI 生成多个选项或分步推进的指令？
□ 评分标准（如适用）：是否提供了明确、无歧义的评估维度？
□ 输出格式：是否指定了期望的输出格式和结构？
□ 稳定性风险：是否识别了最可能失稳的 1-3 个风险？
□ 稳定策略：是否已加入必要的材料隔离、格式控制、few-shot、证据先行、chaining 或工具/检索策略？
```

**统一提示词结构协议**（按需裁剪，不要机械全量输出）：

```text
任务目标：
{用户真正要完成的任务}

非目标：
{本轮明确不做什么，避免范围蔓延}

范围分层：
P0：{本轮必须完成}
P1：{有余力可做}
P2：{未来方向或后续沉淀}

角色与受众：
{AI 应扮演的专业视角，以及最终输出面向谁}

背景与约束：
<context>
{必要背景、边界、语气、合规或风格约束}
</context>

输入材料：
<data>
{待处理材料、变量、用户提供的信息}
</data>

执行步骤：
{必要时写明先做什么、再做什么；复杂任务使用分步或 chaining}

输出格式：
<output_format>
{字段、章节、表格、JSON、XML tag 或其他结构要求}
</output_format>

质量标准：
{判断好结果的标准；事实任务需说明证据要求，不足时允许说明未知}

验收方式：
{如何检查输出是否达成 P0；需要运行测试、引用来源、人工确认或后续动作时写明}
```

**协议使用规则**：
- 简单任务只保留“任务目标 + 输出格式 + 质量标准”
- 复杂任务必须保留“非目标 + P0/P1/P2 + 验收方式”
- 输入材料较长时，必须用 `<data>` 或等价边界隔开
- 风格任务优先加入 few-shot 示例，而不是堆抽象形容词
- 事实高风险任务必须加入证据要求和“不知道/材料未提供”的出口
- 单轮提示过长时，改为 chaining 流程：抽取→分析→输出→质检

**各类别提示词模板**：

#### A. 信息查找类

**A1 预训练知识路径**：
```
[任务描述]
[具体问题]
请基于你的知识回答，如果不确定请明确说明。
```

**A2 网络搜索路径**：
```
[任务描述]
[具体问题]
请通过网络搜索获取最新信息来回答。
优先引用[指定信息源类型，如：官方机构/学术论文/权威媒体]的信息。
```

**A3 深度研究路径**：
```
[任务描述]
[研究问题]
请使用深度研究功能，综合多个信息源进行深入分析。
研究计划应覆盖以下维度：
1. [维度1]
2. [维度2]
3. [维度3]
最终输出一份结构化的研究报告，包含引用来源。
```

#### B. 创意/头脑风暴类

**B1 迭代头脑风暴**：
```
[背景上下文：我是谁、我的情况、我的约束条件]
[我的偏好/不想要的东西]
[具体任务]

请先给我 3-5 个不同的[方案/想法/选项]，每个方案简要说明核心思路。
不要只给常识性建议——结合我提供的[特殊上下文]给出有创意的方案。
```

**B2 决策辅助**：
```
[决策背景]
[选项A] vs [选项B] vs [选项C]

请客观比较以上选项的优缺点，从以下维度评估：
1. [维度1]
2. [维度2]
3. [维度3]

不要假设我偏好某个选项。请基于事实给出分析。
```

#### C. 写作类

**C1 渐进式大纲法（从零写作）**：
```
[写作任务描述]
[目标受众]
[核心观点/论点]
[已有素材/参考资料]

请按以下步骤进行：
第一步：先列出 3 个不同的文章大纲选项，每个大纲包含主要章节和核心论点。
等待我对大纲的反馈后，再进入下一步。
```

**C2 基于已有文本**：
```
[写作任务描述]
[附上已有文本/笔记/素材]

请阅读以上素材，然后：
1. 先列出 3 个不同的文章大纲选项
2. 每个大纲说明如何利用已有素材
等待我的反馈后，再展开。
```

**C3 逐段编辑**：
```
[编辑目标：如更口语化/更专业/更简洁]
[附上文本]

请逐段帮我改写。先处理第一段，给我 2-3 种不同的改写方式。
我选定后再处理下一段。
```

#### D. 批评/评估类

**D1 评分标准驱动法**：
```
[评估对象描述]
[附上作品/方案]

请按以下评分标准进行客观评估：

评分标准（总分 100 分）：
- [维度1]（XX分）：[明确的评判规则，无歧义]
- [维度2]（XX分）：[明确的评判规则，无歧义]
- [维度3]（XX分）：[明确的评判规则，无歧义]

请对每个维度独立打分并说明理由，最后汇总总分。
保持客观，不要因为我想听好话就给高分。
```

#### E. 多模态类

**E1 图像生成**：
```
[场景描述：环境、光线、构图]
[角色/主体描述：外观、姿态、表情]
[风格/氛围：如电影感/水彩/赛博朋克/动漫]
[技术参数：如宽高比、色调]
```

**E2 图像理解**：
```
[附上图片]
[具体问题]
请仔细观察图片中的[指定元素]，回答[具体问题]。
```

**E5 AI视频/图像生成（即梦/可灵）—— 结构化提示词工程法**：

当用户需要为即梦（字节跳动）或可灵（快手）等AI视频/图像生成平台编写提示词时，应用以下结构化框架：

**第一步：确定平台特性**
| 平台 | 定位 | 提示词风格 | 核心公式 |
|------|------|-----------|----------|
| 即梦 | 影视级专业工具 | 讲故事式自然语言 | 镜头+情绪+动作+背景 |
| 可灵 | 大众创作工具 | 公式化结构化 | 主体+运动+背景+镜头+光影+氛围 |

**第二步：选择对应结构模板**

*即梦视频/数字人提示词结构*：
```
镜头[运动方式：推进/拉远/环绕/跟随]，[角色]在[位置]，[情绪状态]，[具体动作]，[说话/唱歌标记]。
[背景事件/其他角色动作]。
[环境互动描述：微风吹过，衣角飘动]。
```

*可灵视频提示词结构*：
```
镜头[镜头语言：中景/特写/跟随]，[光影效果]，[氛围描述]，
[主体细节]在[背景]进行[运动描述]，
[主体间互动]，[画风/质感]。
```

*图像生成通用结构*：
```
[用途/类型：海报/插画/摄影] + [主体描述] + [行为/状态] + 
[环境/背景] + [美学风格：电影质感/水墨/油画] + [技术参数]
```

**第三步：填充核心要素**

| 要素类别 | 即梦侧重 | 可灵侧重 | 示例 |
|---------|---------|---------|------|
| 镜头语言 | 精确控制起始和结束状态 | 指定镜头类型即可 | 镜头从侧面缓慢推进到正面中景 |
| 情绪表达 | 详细描述内心状态和微表情 | 简要说明氛围 | 心情平静，微笑着；不屑地翻白眼 |
| 动作编排 | 使用时间顺序词（先...然后...） | 描述运动状态 | 先向前走，然后停下脚步 |
| 环境互动 | 强调背景事件和氛围变化 | 简要描述背景 | 背景中灯光柔和地闪烁 |
| 光影氛围 | 融入叙事描述 | 作为独立要素 | 氛围光照，电影级调色 |

**第四步：应用进阶技巧**

*多模态引用（即梦Seedance 2.0）*：
```
@图片1 @图片2 中的[主体]在@视频1 的[场景/节奏]中[动作]
```

*时间轴描述（智能多帧）*：
```
[0-5秒]：[画面状态]，[动作]，（情绪：[状态]）
[6-10秒]：[画面状态]，[动作]，（情绪：[状态]）
```

*分镜式描述*：
```
[时长]：[分镜描述]，[镜头运动]，[角色动作/台词]
```

**第五步：关键词优化**

*影像风格关键词*：大特写、特写、近景、中景、全景、大远景、浅景深、动态模糊、柔焦

*艺术风格关键词*：国画、水墨、工笔、油画、水彩、版画、极简主义、CG概念、皮克斯动画

*光影氛围关键词*：氛围光照、晨光、暖色调、冷色调、强烈对比、明暗对比（chiaroscuro）、电影级调色

*材质效果关键词*：亚麻、牛仔、毛绒、皮革、陶瓷、玻璃、金属、石膏、大理石

**第六步：质量检查**

```
□ 画面内容是否简单明确（符合视频时长内可完成的动作）
□ 运动是否符合物理规律（避免复杂物理运动如球类弹跳）
□ 关键词是否具体（使用"日系青春电影质感"而非"好看的风格"）
□ 文字内容是否用引号标注（提升文字准确率）
□ 是否指定了使用场景（海报/插画/摄影/短视频）
```

#### F. 代码/应用类

**F1 构建应用**：
```
请构建一个[应用类型]。

目标：[用户能做什么]
输入：[用户需要提供什么]
输出：[用户看到什么]

技术要求：
- [如有：使用特定框架/语言]
- [如有：响应式设计/特定功能]
```

**F2 数据分析**：
```
[附上数据文件]

请分析以上数据，回答以下问题：
1. [问题1]
2. [问题2]

请通过编写和运行代码来进行分析，并生成可视化图表。
```

#### G. 推理/复杂任务类

**G1 长时间推理**：
```
[任务描述]
[附上所有相关上下文/文件]

请认真思考这个问题。阅读所有材料，在回答前深入思考。
[如需要：请从以下维度进行分析：维度1、维度2、维度3]
```

---

### 阶段四：验收与后续动作

**目标**：在提示词生成或任务执行后，检查 P0 是否完成、是否越界、是否需要验证、是否需要沉淀到 wiki / records / outputs / skill / automation，或只留在对话中。

**验收概述格式**：

```markdown
## 任务验收概述

| 检查项 | 状态 | 说明 |
|--------|------|------|
| P0完成度 | ✅/⚠️ | [本轮必须完成的工作是否完成] |
| 非目标遵守 | ✅/⚠️ | [是否避免了范围蔓延] |
| 上下文充分性 | ✅/⚠️ | [说明] |
| 谄媚风险 | ✅/⚠️ | [说明] |
| 任务明确性 | ✅/⚠️ | [说明] |
| 信息路径 | ✅/⚠️ | [说明] |
| 迭代机制 | ✅/⚠️ | [说明] |
| 输出格式 | ✅/⚠️ | [说明] |
| 失败风险 | ✅/⚠️ | [最可能失稳的 1-3 个点] |
| 稳定策略 | ✅/⚠️ | [已采用的 Anthropic 稳定策略] |
| thinking-extend | ✅/⚠️ | [已执行默认收尾判断 / 跳过原因] |
| 后续动作 | ✅/⚠️ | [无需沉淀 / 保存 outputs / 更新 wiki / 更新 skill / 创建自动化 / 需要用户确认] |

**提示词类别**：[如 C1 + A2]
**推荐使用模型**：[如 GPT-4o / Claude 3.5 / Gemini]
**预估效果**：[如：该提示词采用渐进式大纲法，预计能生成结构清晰的文章框架，但最终文本质量取决于后续迭代轮次]
**优化建议**：[如有改进空间，给出 1-2 条建议]
```

**自检标准详解**：

| 检查项 | 通过标准 | 常见问题 |
|--------|----------|----------|
| P0完成度 | 本轮必须交付的结果已经完成或明确说明阻塞 | 做了很多 P1/P2，但 P0 没完成 |
| 非目标遵守 | 没有擅自扩展到用户未确认的产物、规则或长期资产 | 单篇阅读自动变成 wiki/skill/报告全套改造 |
| 上下文充分性 | 提示词包含目标受众、任务背景、约束条件 | 缺少受众信息、缺少素材引用 |
| 谄媚风险 | 措辞中性，不暗示期望答案 | "帮我证明..."、"你觉得...好不好" |
| 任务明确性 | AI 能清楚知道要做什么、输出什么格式 | 任务模糊、缺少输出格式要求 |
| 信息路径 | 与任务复杂度匹配 | 简单问题用深度研究、复杂问题不用搜索 |
| 迭代机制 | 包含多选项生成或分步推进指令 | 一次性要求输出最终结果 |
| 输出格式 | 指定了输出结构/长度/风格 | 未指定格式导致输出不可控 |
| 失败风险 | 已识别任务边界、材料隔离、格式、事实、风格、推理、工具等主要风险 | 只判断任务类型，没有判断会在哪里失稳 |
| 稳定策略 | 已针对风险加入 clear/direct、XML 分隔、few-shot、证据先行、chaining 或工具/检索 | 策略堆叠过多，或高风险任务没有加证据与格式约束 |
| thinking-extend | 每个完成的非机械任务都完成默认收尾判断；若跳过，明确跳过条件 | 主任务交付后直接结束，忘记做 post-task reflection |
| 后续动作 | 对结果归属有判断：对话、outputs、wiki、records、skill、automation 或无需沉淀 | 所有任务都强行入库，或值得沉淀的经验没有记录 |

**后续动作判断**：
- `thinking-extend`：所有完成的非机械任务都必须先执行默认收尾判断。除非用户明确关闭、任务纯机械、wiki无足够关联、或当前对话明确只要结论，否则输出最小结构：任务回看、关联发现、弱关联观察、1个延伸问题。
- `no promotion`：一次性回答、临时分析、无长期价值。
- `outputs`：运行产物、网页转换、草稿、临时报告。
- `wiki/sources`：外部文章、报告、访谈等来源摘要。
- `wiki/concepts/entities/products`：稳定概念、实体、产品分析或长期知识页。
- `records/facts`：可复用、可验证的工作空间事实。
- `skill update`：重复流程、方法论升级、工具协议变化；重大变更需用户确认。
- `automation`：周期性任务或监控需求；创建前必须确认周期、触发条件和停止条件。

---

## 完整执行示例

### 示例 1：用户说"帮我写个提示词，让 AI 帮我分析竞品"

**阶段一（反向沟通）**：
> "你想让 AI 分析哪些竞品？你手头有竞品的相关资料吗（产品页面、定价、功能列表）？分析结果用于什么场景（内部决策 / 投资路演 / 市场报告）？"

**阶段二（类别判断）**：
> "你的需求属于 **A2（网络搜索）+ D1（客观评估）** 复合类别。需要先搜索竞品信息，再用评分标准进行客观比较。"

**阶段三（生成提示词）**：
> （应用 A2 + D1 模板生成完整提示词）

**阶段四（验收与后续动作）**：
> （输出自检表格，标注状态和建议）

---

### 示例 2：用户说"帮我写个即梦提示词，生成一个女生在咖啡厅的视频"

**阶段一（反向沟通）**：
> "你想用即梦的哪个模型？Seedance 1.5 Pro（支持环境音效/人声对白）还是 Seedance 2.0（多模态参考）？视频时长要求？画幅比例？是否需要口型同步？"

**阶段二（类别判断）**：
> "你的需求属于 **E5（AI视频/图像生成 - 即梦）** 类别。需要应用即梦的'讲故事式'提示词结构：镜头运动 + 情绪状态 + 具体动作 + 背景事件。"

**阶段三（生成提示词）**：

*原始需求*："一个女孩在咖啡厅"

*优化后提示词*：
```
镜头从侧面缓慢推进到正面中景。年轻女子坐在窗边，心情平静，
轻轻端起咖啡杯，微笑着对镜头说话。旁边的男孩悄悄看着她，
时不时转向镜头笑笑。背景中灯光柔和地闪烁，氛围温暖惬意。
```

*结构拆解*：
| 要素 | 内容 |
|------|------|
| 镜头运动 | 从侧面缓慢推进到正面中景 |
| 角色情绪 | 心情平静、微笑着 |
| 具体动作 | 轻轻端起咖啡杯 |
| 背景事件 | 旁边的男孩悄悄看着 |
| 环境互动 | 灯光柔和地闪烁 |

**阶段四（验收与后续动作）**：

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 上下文充分性 | ✅ | 包含镜头、情绪、动作、背景完整要素 |
| 平台适配性 | ✅ | 符合即梦"讲故事式"提示词风格 |
| 动作可行性 | ✅ | 画面内容简单，符合10-15s时长可完成的动作 |
| 关键词具体性 | ✅ | 使用"缓慢推进""柔和地闪烁"等具体描述 |
| 物理合理性 | ✅ | 无复杂物理运动，符合平台能力范围 |

**推荐模型**：即梦 Seedance 1.5 Pro（如需人声对白）或 Seedance 2.0（如需多模态参考）
**预估效果**：该提示词采用即梦推荐的叙事结构，预计能生成具有电影质感的视频，角色动作自然，氛围温暖
**优化建议**：如需更精细控制，可添加"先...然后..."描述动作顺序；如需环境音效，可在提示词末尾添加"背景音为轻柔的爵士乐"

---

### 示例 3：用户说"帮我写个可灵提示词，生成一只熊猫看书的视频"

**阶段一（反向沟通）**：
> "你想用可灵的哪个功能？文生视频还是图生视频？视频时长？画幅比例（16:9/9:16/1:1）？是否需要运镜控制？"

**阶段二（类别判断）**：
> "你的需求属于 **E5（AI视频/图像生成 - 可灵）** 类别。需要应用可灵的公式化提示词结构：主体 + 运动 + 背景 + 镜头语言 + 光影 + 氛围。"

**阶段三（生成提示词）**：

*原始需求*："一只熊猫在看书"

*优化后提示词*：
```
镜头中景拍摄，背景虚化，氛围光照，一只大熊猫戴着黑框眼镜
在咖啡厅看书，书本放在桌子上，桌子上还有一杯咖啡，冒着热气，
旁边是咖啡厅的窗户，电影级调色。
```

*结构拆解*：
| 要素 | 内容 |
|------|------|
| 镜头语言 | 中景拍摄，背景虚化 |
| 光影 | 氛围光照 |
| 主体 | 大熊猫戴着黑框眼镜 |
| 运动 | 在咖啡厅看书 |
| 背景 | 咖啡厅的窗户 |
| 氛围 | 电影级调色 |

**阶段四（验收与后续动作）**：

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 上下文充分性 | ✅ | 包含可灵六大要素完整结构 |
| 平台适配性 | ✅ | 符合可灵公式化提示词风格 |
| 动作可行性 | ✅ | "看书"动作简单稳定，适合AI生成 |
| 关键词具体性 | ✅ | 使用"黑框眼镜""冒着热气"等细节描述 |
| 物理合理性 | ✅ | 无复杂物理运动 |

**推荐功能**：可灵图生视频（先上传熊猫图片）或文生视频
**预估效果**：该提示词采用可灵标准公式，预计能生成风格统一、氛围温馨的视频
**优化建议**：如需运镜，可添加"镜头缓慢推进"或选择运镜控制功能

---

## 特殊场景处理

### 场景 1：用户需求模糊
- 不猜测用户意图，使用 AskUserQuestion 明确
- 提供 2-3 个可能的方向让用户选择

### 场景 2：用户已有提示词，想优化
- 跳过阶段一（素材已隐含在现有提示词中）
- 先执行阶段1.1稳定性风险扫描，判断提示词最可能在哪里失稳
- 再对现有提示词执行阶段四的自检
- 基于自检结果选择 `improve_prompt`，优先补强任务边界、材料隔离、输出格式、few-shot、证据约束或 chaining

### 场景 3：用户需要跨模型通用提示词
- 提示词应避免模型特定功能（如特定工具名）
- 使用通用指令（"请搜索"而非"使用深度研究"）

### 场景 4：用户需要提示词给别人用
- 在提示词中加入使用说明（如何填写变量、预期效果）
- 标注推荐模型和参数设置

---

## 与其他技能的协作

| 协作技能 | 协作方式 |
|----------|----------|
| **毛选方法论技能库** | 阶段1.5维度判断后，自动加载匹配的思维框架（矛盾分析法/调查研究法/群众路线等），融入后续生成阶段 |
| task-spec-capsule | 阶段1.0使用，负责目标、非目标、P0/P1/P2、成功标准、开放问题和上下文路径 |
| loop-workflow-patterns | 阶段2.5使用，负责判断任务是否需要 loop，并选择 workflow pattern、验证层、停止条件、预算和沉淀路径 |
| Anthropic 提示工程方法 | 阶段1.1稳定性风险扫描后，按失败模式加载 clear/direct、data separation、formatting、few-shot、evidence-first、chaining、tool/retrieval 等稳定策略 |
| hv-analysis | 当用户需要产品分析类提示词时，参考 hv-analysis 的分析框架 |
| hotspot-radar | 当用户需要 AI 行业信息查找类提示词时，建议使用 hotspot-radar 内置的 AI HOT 数据源 |
| oral-copy | 当用户需要口播文案或公众号长文创作类提示词时，参考 oral-copy 的文案公式 |
| neat-freak | 会话结束时，将本次提示词决策经验同步到记忆 |
| darwin-skill | 可用于优化 prompt-flow 及其他技能的质量 |
| knowledge-extractor | 当用户需要先抽问题、抽引用或补标签时，优先用结构化提取替代松散读材料 |

**与毛选方法论技能库的深度整合**：

阶段1.5（维度判断）是 prompt-flow 与毛选技能库的核心连接点，但它是按需加载层，不再对简单任务强制外显。匹配规则：

| 用户任务特征 | 匹配维度 | 加载技能 | 融入方式 |
|-------------|----------|----------|----------|
| 问题复杂、千头万绪 | 战略思维 | 矛盾分析法 | 在生成阶段用"找出主要矛盾"框架组织回答 |
| 面对强大对手、资源有限 | 战略思维 | 持久战战略 | 在生成阶段用"三阶段规划"框架制定策略 |
| 对新领域不了解 | 工作方法 | 调查研究法 | 在反向沟通阶段强化信息收集 |
| 需要推动变革、阻力大 | 工作方法 | 群众路线 | 在生成阶段用"从群众中来"框架设计执行方案 |
| 需要建立广泛联盟 | 战略思维 | 统一战线策略 | 在生成阶段用"三类势力分析"框架 |
| 新方案需要验证 | 工作方法 | 试点推广法 | 在生成阶段用"由点到面"框架设计实施路径 |
| 积小胜为大胜 | 军事指挥 | 游击战十六字诀 | 在生成阶段用"敌进我退"框架设计竞争策略 |

**组合使用示例**：

- **"创业从0到1"** → 调查研究法 + 矛盾分析法 + 群众路线 + 持久战战略
- **"面对强大竞争对手"** → 阶级分析法 + 统一战线策略 + 游击战十六字诀 + 运动战原则
- **"产品重大改版"** → 试点推广法 + 群众路线 + 调查研究法 + 矛盾分析法

**与 oral-copy 的深度整合**：
当用户需要为 AI 视频/图像生成平台（如即梦、可灵）编写提示词时，prompt-flow 的 E5 类别会与 oral-copy 协同工作：
- prompt-flow 负责提示词的结构设计和优化流程
- oral-copy 提供影视级叙事的语言风格和节奏控制技巧
- 两者结合可生成既符合平台技术要求、又具有专业影视质感的提示词

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2026-05-09 | 初始版本，基于 Andrew Ng《AI Prompting for Everyone》全三模块课程 |
| 1.1 | 2026-05-09 | 升级为全局固定触发（`trigger: "fixed"`），优化触发逻辑 |
| 1.2 | 2026-05-10 | 整合 AI 视频/图像生成提示词策略（即梦/可灵），新增 E5 类别和完整示例 |
| 1.3 | 2026-05-11 | 新增阶段1.5「维度判断」，整合毛选方法论技能库（10个skill），四阶段升级为五阶段流程 |
| 1.4 | 2026-05-19 | 增加 Fabric 风格增强动作：`improve_prompt` 与 `suggest_pattern` |
| 1.5 | 2026-05-20 | 融合 Anthropic 交互式提示工程教程，新增阶段1.1稳定性风险扫描、稳定策略映射、统一提示词结构协议和自检增强 |
| 1.6 | 2026-06-06 | 融合 write-spec / PRD framing，新增任务规格胶囊、非目标、P0/P1/P2范围分层、上下文路径、验收与后续动作；维度判断改为按需加载 |
| 1.6.1 | 2026-06-06 | 在验收与后续动作中显式加入 thinking-extend 默认收尾判断，防止主任务交付后遗漏 post-task reflection |
| 1.7 | 2026-06-06 | 轻量嵌入 loop-flow reference，新增阶段2.5 Loop/Workflow 判断，用于复杂任务的循环、并行、独立验证、停止条件和预算判断 |

## Fabric 融合增强

从 2026-05-19 起，这个 skill 增加两个可选增强动作，专门解决“需求已经清楚，但 prompt 还不够锋利”和“用户其实是在找该用哪个 skill / workflow”的问题。需要时先读 `references/fabric-routing-pack.md`。

优先使用的 Fabric 风格模块：
- `improve_prompt`：把已有 prompt 打磨成更稳的执行指令
- `suggest_pattern`：直接吸收原 `suggest-skill` 的路由职责，把“这个任务该怎么做”映射成更合适的 skill / workflow

使用原则：
- 不要因为能优化 prompt 就跳过前置澄清
- 当用户是在问“该用什么方法 / 该怎么设计流程”时，优先走 route / suggest 动作
- 当用户已经有 prompt 草稿时，优先走 refine 动作，并先用阶段1.1判断失败风险
