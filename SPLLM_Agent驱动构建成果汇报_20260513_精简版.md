# SPLLM 工作空间 · Agent/AI 驱动构建成果汇报（精简版）

> **文档版本**：v1.1 (精简版)
> **生成日期**：2026-05-13
> **字数**：约 1000 字
> **汇报对象**：项目相关方
> **工作空间**：`E:\SPLLM\`

---

## 一、项目概述

**SPLLM（Hotpoint Live-Video LLM Wiki）** 是一个由 LLM Agent 驱动的知识管理系统，核心目标是将碎片化的市场信息、深度资料和创意素材，转化为结构化、可关联、可行动的知识资产，并支持概念产品的系统化孵化。

**核心定位**：不是被动的知识仓库，而是主动的 **AI Agent 运营的知识工坊**。

**设计哲学**：
- **LLM-first**：维基主要由 LLM 通过对话维护
- **知识网络化**：页面之间通过 Wikilink 建立双向关联
- **来源可追溯**：每条知识都有原始来源支撑
- **技能强制固化**：同一件事做 2 次以上，必须写成 Skill

---

## 二、核心痛点与解决方案

| 痛点 | 表现 | AI 驱动解决方案 |
|------|------|----------------|
| **信息碎片化** | 市场动态、文章、笔记分散在多个目录，无统一入口 | Layer 1-4 四层架构，自动摄取与分类 |
| **知识孤岛** | 不同来源的信息无法建立关联，洞察孤立 | Wikilink 双向链接 + 自动交叉引用 |
| **重复劳动** | 同类任务（如研报分析、文案写作）反复手动执行 | 技能系统（Skill）+ 四条铁律，强制将重复工作固化为技能 |
| **分析深度不足** | 人工分析容易浅尝辄止，缺乏系统方法论 | hv-analysis 横纵分析法，强制纵向+横向+交汇三段式深度研究 |
| **输出质量不稳定** | 不同时间、不同状态下输出质量差异大 | 技能内置方法论 + 质检清单 + Darwin 自动化优化循环 |

---

## 三、核心架构与逻辑流

### 3.1 四层知识架构

```
┌─────────────────────────────────────────────────┐
│  Layer 4: Schema（规范层）                        │
│  SCHEMA.md / CLAUDE.md / AGENTS.md              │
│  定义维基的结构、约定、工作流程（Agent 操作手册）    │
├─────────────────────────────────────────────────┤
│  Layer 3: Wiki（知识层）                          │
│  entities/ concepts/ comparisons/ products/      │
│  结构化的知识页面，通过 Wikilink 互联               │
├─────────────────────────────────────────────────┤
│  Layer 2: Source（来源层）                        │
│  wiki/sources/ + raw/                            │
│  原始材料的结构化摘要，保留核心信息和元数据           │
├─────────────────────────────────────────────────┤
│  Layer 1: Daily Feed（信息层）                     │
│  raw/ book/ books/ source-material/               │
│  未加工的市场信息、文章、笔记、案例、书籍等           │
└─────────────────────────────────────────────────┘
```

**信息流向**：Layer 1 → Layer 2 → Layer 3，逐层提炼。Layer 4 规范所有层的操作。

### 3.2 五大核心工作流

1. **Ingest（摄取）**：信息层 → 来源层，创建 source 摘要并建立 Wikilink
2. **Digest（消化）**：深度资料的方法论级理解，创建结构化摘要
3. **Query（查询）**：基于 Wiki 回答问题，整合信息并标注引用
4. **Produce（生产）**：洞察转化为可交付输出物（Markdown/PDF/演示文稿）
5. **Check（健康检查）**：定期审查内容一致性、结构完整性、链接有效性

---

## 四、长链推理（Long-Chain Reasoning）实现机制

### 4.1 hv-analysis：横纵分析法（三段式长链推理）

**推理链长度**：3 段 × 平均 5000 字 = 约 16000-40000 字深度输出

- **第一段：纵向分析**（时间轴长链推理）
  - 收集研究对象从诞生到当下的完整生命历程
  - 沿时间轴还原关键转折点、驱动因素、因果关系
  - 输出：6000-15000 字叙事体分析

- **第二段：横向分析**（截面式比较推理）
  - 收集当前时间截面上竞品/同类产品的全面数据
  - 多维度并排比较（功能/体验/商业模式/市场表现）
  - 输出：3000-10000 字对比分析

- **第三段：横纵交汇**（综合推理 + 未来推演）
  - 纵向脉络 + 横向格局 → 独到洞察
  - 基于交汇洞察，给出未来 1-3 年趋势判断
  - 输出：1500-3000 字综合报告

**质量保障**：每个段落强制引用 Wikilink 来源，使用 JSON Schema 约束输出结构，三段式质检清单。

### 4.2 book2skill：RIA-TV++ 六阶段蒸馏流水线

1. **Stage 0**: Adler 整体理解（输入全书 → 输出 BOOK_OVERVIEW.md）
2. **Stage 1**: 5 个 Agent 并行提取（框架/原则/案例/反例/术语）
3. **Stage 1.5**: 三重验证筛选（跨域佐证/预测力/独特性，通过率 25-50%）
4. **Stage 2**: RIA++ 构造 Skill（Reading/Interpretation/Application/Boundary）
5. **Stage 3**: Zettelkasten 链接（新 skill 与已有 skill 的关系链接）
6. **Stage 4**: 压力测试（用 test-prompts.json 实际跑一遍，评估输出质量）

### 4.3 Darwin Skill：自动化优化循环（自我进化推理链）

**推理链**：评估 → 改进 → 实测验证 → 决策（保留/回滚）→ 循环

- **Phase 1**: 基线评估（结构评分 + 效果评分，总分 100）
- **Phase 2**: 优化循环（棘轮机制，只保留改进，自动回滚退步，最多 10 轮）
- **五条核心原则**：单一可编辑资产、双重评估、棘轮机制、独立评分、人在回路

---

## 五、多 Agent 协作机制

### 5.1 技能系统：专业化 Agent 分工

每个 Skill 本质上是一个 **专业 Agent 的行为定义**，主 Agent 根据任务类型调用对应的 Skill，实现"多专家协作"。

| Skill | 角色定位 | 核心能力 |
|-------|---------|---------|
| **hv-analysis** | 行业研究员 | 横纵分析法，产出 16000-40000 字深度报告 |
| **prompt-flow** | 提示词工程师 | 决策任务类别，生成高质量提示词（全局前置触发） |
| **oral-copy** | 文案创作专家 | 4 类文案（数据活人感/人生感悟/产品种草/公众号长文） |
| **book2skill** | 知识蒸馏工程师 | RIA-TV++ 六阶段流水线，将书转化为 skill |
| **darwin-skill** | 技能优化工程师 | 8 维度评估 + 棘轮优化循环 |

### 5.2 并行子 Agent 协作（以 hv-analysis 为例）

```
用户：研究一下泡泡玛特

主 Agent（协调者）
  ├── 启动 子 Agent 1（纵向信息收集）→ 泡泡玛特发展历程、关键转折点
  ├── 启动 子 Agent 2（横向信息收集）→ 潮玩行业竞品（52TOYS、寻找独角兽...）
  ├── 启动 子 Agent 3（补充信息收集）→ 用户需求趋势、行业分析、消费心理
  ├── [并行等待，所有子 Agent 完成后]
  ├── 主 Agent 整合三方信息
  │     ├── 纵向分析（基于子 Agent 1 的数据）
  │     ├── 横向分析（基于子 Agent 2 的数据）
  │     └── 横纵交汇（综合 1+2+3，产出洞察）
  └── 生成 Markdown 报告 → 转 PDF → 保存
```

### 5.3 全局前置协作：prompt-flow 的调度角色

**prompt-flow** 被设计为 **全局前置触发**，每次用户发送消息时首先执行，相当于一个"任务路由 Agent"：

1. **Step 1**: 反向沟通（向用户收集素材需求、使用场景）
2. **Step 2**: 类别判断（匹配任务类别：A 信息查找/B 头脑风暴/C 写作/D 批评/E 多模态/F 代码/G 推理）
3. **Step 3**: 生成回答/提示词
4. **Step 4**: 自检概述（上下文充分性、任务明确性、方法论匹配度）

### 5.4 人在回路（Human-in-the-Loop）协作

所有长链推理流程都设计了 **人工审核节点**，确保 AI 不会偏离用户真实需求：

- book2skill：Stage 0 完成后、Stage 2 开始前，确认 BOOK_OVERVIEW.md 是否准确
- darwin-skill：每个 skill 优化完成后暂停等人类确认，Phase 2.5 探索性重写前必须用户明确批准
- neat-freak：五步流程的"实际修改"步骤前，展示变更影响矩阵，用户确认后再执行

---

## 六、具体成果展示

### 6.1 Wiki 知识库现状（截至 2026-05-13）

| 指标 | 数值 |
|------|------|
| **总页面数** | 97 |
| **来源摘要页面** | 96（daily-feed 15 + raw 79 + book 1 + books 1） |
| **产品研究报告** | 按需生成（hv-analysis 技能就绪） |

**来源分布**：
- daily-feed：15 个（AI 视频/图像生成平台使用手册）
- raw：79 个（品牌营销/用户研究/商业模式/AI技术/营销趋势等 8 大类）
- book/books：2 个（毛泽东选集，已蒸馏出 10 个核心技能）

### 6.2 技能生态（7 个已安装技能）

| 技能 | 版本 | 核心方法论 | 状态 |
|------|------|-----------|------|
| hv-analysis | v2.0 | 横纵分析法（纵向+横向+交汇） | ✅ 已安装 |
| prompt-flow | v1.2 | 四阶段决策 + 7 大提示词类别 | ✅ 已安装（全局前置） |
| oral-copy | v2.0 | 4 类文案公式 + HKR 选题法 | ✅ 已安装 |
| khazix-writer | v1.0 | 5 种文章原型 + 风格内核 | ✅ 已安装 |
| book2skill | v1.0 | RIA-TV++ 六阶段蒸馏流水线 | ✅ 已安装 |
| darwin-skill | v1.0 | 8 维度评估 + 棘轮优化循环 | ✅ 已安装 |
| neat-freak | v1.0 | 五步同步流程 + 变更影响矩阵 | ✅ 已安装 |

### 6.3 概念产品孵化成果

| 产品 | 状态 | 产出物 |
|------|------|---------|
| **PetRide「宠行」** | 概念设计阶段 | 产品定义文档 + AI 生成渲染图 |
| **智能土豆种植器** | hv-analysis 测试 | 横纵分析报告（Markdown + PDF） |
| **无水生菜种植机** | hv-analysis v2.0 完整测试 | 横纵分析报告含商业化六维度 |

---

## 七、总结与展望

### 7.1 核心成果总结

SPLLM 工作空间通过 **Agent/AI 驱动** 实现了从碎片化信息到结构化知识的系统化转化，核心成果包括：

1. **知识管理架构**：四层架构（Schema/Wiki/Source/Daily Feed）确保信息可追溯、可关联
2. **方法论体系**：7 个技能覆盖分析/写作/蒸馏/优化全流程，强制固化重复工作
3. **长链推理能力**：hv-analysis 三段式（16000+ 字）、book2skill 六阶段、darwin 优化循环
4. **多 Agent 协作**：主 Agent 协调 + 专业化 Skill Agent + 并行子 Agent，实现复杂任务的分工与整合
5. **概念孵化支持**：从产品定义到 AI 渲染图的全流程支持

### 7.2 下一步规划

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P0 | 从 96 个来源摘要中批量提取 entity/concept 页面 | Wiki 知识图谱补全 |
| P0 | PetRide「宠行」概念车：完成多风格 AI 渲染图 | 支持众筹/融资材料准备 |
| P1 | 配置 AI HOT 日报自动化定时任务 | 每日自动获取 AI 行业动态 |
| P1 | 使用 darwin-skill 对所有 7 个技能进行质量优化 | 提升技能输出稳定性 |
| P2 | 将 KS2026 护生短视频自动化工作流文档化 | 固化为 Skill |

---

*本文档由 LLM Agent 基于工作空间实际文件（CLAUDE.md / AGENTS.md / index.md）自动生成，精简版约 1000 字。*
