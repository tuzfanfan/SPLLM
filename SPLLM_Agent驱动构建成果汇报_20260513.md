# SPLLM 工作空间 · Agent/AI 驱动构建成果汇报

> **文档版本**：v1.0
> **生成日期**：2026-05-13
> **汇报对象**：项目相关方
> **工作空间**：`E:\SPLLM\`

---

## 一、项目概述

**SPLLM（Hotpoint Live-Video LLM Wiki）** 是一个由 LLM Agent 驱动的知识管理系统，核心目标是将碎片化的市场信息、深度资料和创意素材，转化为结构化、可关联、可行动的知识资产，并支持概念产品的系统化孵化。

**核心定位**：不是被动的知识仓库，而是主动的 **AI Agent 运营的知识工坊**。

---

## 二、核心痛点与解决方案

### 2.1 解决的核心痛点

| 痛点 | 表现 | AI 驱动解决方案 |
|------|------|----------------|
| **信息碎片化** | 市场动态、文章、笔记分散在多个目录，无统一入口 | Layer 1-4 四层架构，自动摄取与分类 |
| **知识孤岛** | 不同来源的信息无法建立关联，洞察孤立 | Wikilink 双向链接 + 自动交叉引用 |
| **重复劳动** | 同类任务（如研报分析、文案写作）反复手动执行 | 技能系统（Skill）+ 四条铁律，强制将重复工作固化为技能 |
| **分析深度不足** | 人工分析容易浅尝辄止，缺乏系统方法论 | hv-analysis 横纵分析法，强制纵向+横向+交汇三段式深度研究 |
| **输出质量不稳定** | 不同时间、不同状态下输出质量差异大 | 技能内置方法论 + 质检清单 + Darwin 自动化优化循环 |
| **知识来源不可追溯** | 结论不知从何而来，无法验证 | 每个 Wiki 页面强制 Frontmatter + sources 字段，可回溯到 raw/ 原始文件 |

### 2.2 设计哲学

| 原则 | 说明 |
|------|------|
| **LLM-first** | 维基主要由 LLM 通过对话维护，人类通过 Obsidian 辅助浏览 |
| **知识网络化** | 页面之间通过 Wikilink 建立双向关联，形成图谱而非孤立文档 |
| **来源可追溯** | 每条知识都有原始来源支撑，可回溯到 raw/ 目录中的原始文件 |
| **渐进式构建** | 从 raw → source → entity/concept → product，逐层提炼 |
| **模板驱动** | 所有页面类型都有标准模板，确保结构一致性 |
| **技能强制固化** | 同一件事做 2 次以上，必须写成 Skill（四条铁律） |

---

## 三、核心架构与逻辑流

### 3.1 四层知识架构

```
┌─────────────────────────────────────────────────┐
│  Layer 4: Schema（规范层）                        │
│  SCHEMA.md / CLAUDE.md / AGENTS.md              │
│  定义维基的结构、约定、工作流程（Agent 操作手册）    │
├─────────────────────────────────────────────────┤
│  Layer 3: Wiki（知识层）                          │
│  entities/ concepts/ comparisons/ products/      │
│  结构化的知识页面，通过 Wikilink 互联               │
├─────────────────────────────────────────────────┤
│  Layer 2: Source（来源层）                        │
│  wiki/sources/ + raw/                            │
│  原始材料的结构化摘要，保留核心信息和元数据           │
├─────────────────────────────────────────────────┤
│  Layer 1: Daily Feed（信息层）                     │
│  raw/ book/ books/ source-material/               │
│  未加工的市场信息、文章、笔记、案例、书籍等           │
└─────────────────────────────────────────────────┘
```

**信息流向**：Layer 1 → Layer 2 → Layer 3，逐层提炼。Layer 4 规范所有层的操作。

### 3.2 五大核心工作流（逻辑链）

```
用户指令
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ Step 1: Ingest（摄取） — 信息层 → 来源层          │
│ raw/ 新文件 → 阅读分析 → 提取实体/概念/数据        │
│   → 创建 source 摘要 → 更新 entity/concept 页面   │
│   → 建立 Wikilink 交叉引用 → 更新 index + log    │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ Step 2: Digest（消化） — 深度资料的方法论级理解     │
│ source-material/ 深度文章 → 理解方法论框架          │
│   → 与用户讨论应用 → 创建结构化摘要               │
│   → 更新 concept 页面（增加方法论、可操作步骤）    │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ Step 3: Query（查询） — 基于 Wiki 回答问题        │
│ 用户问题 → 搜索 index.md 定位相关页面             │
│   → 阅读 entity/concept/source 页面               │
│   → 整合信息，形成答案 → 标注 Wikilink 引用       │
│   → 可选：保存为 Wiki 页面 → 更新 index + log    │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ Step 4: Produce（生产） — 洞察转化为可交付输出物   │
│ Wiki 知识 + 用户需求 → Markdown 文档 / Marp 演示  │
│   → 图表可视化 / 设计画布                         │
│   → hv-analysis 深度研究报告（横纵分析法）         │
│   → PDF 生成（fpdf2，Windows 环境）              │
└─────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────┐
│ Step 5: Check（健康检查） — 定期质量审查           │
│ 内容一致性 → 结构完整性 → 链接有效性               │
│   → 元数据完整性 → 质量评估                       │
│   → 输出健康检查报告 + 修复建议                   │
└─────────────────────────────────────────────────────┘
```

---

## 四、长链推理（Long-Chain Reasoning）实现机制

本工作空间的核心优势之一是 **多条长链推理流水线的系统化实现**，确保 Agent 在分析、创作、优化任务中都能进行深度、可追溯的推理。

### 4.1 hv-analysis：横纵分析法（三段式长链推理）

**推理链长度**：3 段 × 平均 5000 字 = 约 16000-40000 字深度输出

```
第一段：纵向分析（时间轴长链推理）
  ├── 收集：研究对象从诞生到当下的完整生命历程
  ├── 推理：沿时间轴还原关键转折点、驱动因素、因果关系
  ├── 输出：6000-15000 字叙事体分析
  └── 验证：每个关键节点必须有来源支撑

第二段：横向分析（截面式比较推理）
  ├── 收集：当前时间截面上竞品/同类产品的全面数据
  ├── 推理：多维度并排比较（功能/体验/商业模式/市场表现）
  ├── 输出：3000-10000 字对比分析
  └── 验证：使用并列表格，确保比较维度一致

第三段：横纵交汇（综合推理 + 未来推演）
  ├── 综合：纵向脉络 + 横向格局 → 独到洞察
  ├── 推演：基于交汇洞察，给出未来 1-3 年趋势判断
  ├── 输出：1500-3000 字综合报告
  └── 验证：洞察必须有前两段的事实支撑，不得凭空推断
```

**长链推理的质量保障**：
- 每个段落强制引用 Wikilink 来源
- 使用 JSON Schema（v2.0）约束输出结构
- 三段式质检清单（各段独立自检 → 交叉验证 → 综合评分）

### 4.2 book2skill：RIA-TV++ 六阶段蒸馏流水线

**推理链长度**：6 个阶段，每个阶段包含多个推理步骤

```
Stage 0: Adler 整体理解
  → 输入：书籍全文 → 输出：BOOK_OVERVIEW.md（整书骨架）

Stage 1: 5 个 Agent 并行提取
  → 输入：BOOK_OVERVIEW + 全书文本
  → 5 个并行推理器：
     ① framework-extractor  → 提取框架/模型
     ② principle-extractor  → 提取原则/准则
     ③ case-extractor        → 提取案例/应用场景
     ④ counter-example      → 提取反例/边界条件
     ⑤ glossary-extractor   → 提取术语/定义
  → 输出：candidates/ 候选方法论单元池

Stage 1.5: 三重验证筛选
  → 推理：每个候选单元必须通过三道门
     ① 跨域佐证：是否适用于多个领域？
     ② 预测力：是否能预测未知现象？
     ③ 独特性：是否与已有 skill 重复？
  → 通过率：通常 25-50%
  → 输出：通过的单元（写入 rejected/ 供审计）

Stage 2: RIA++ 构造 Skill
  → 推理：对每个通过单元，构造完整的 SKILL.md
     R: Reading（阅读建议）
     I: Interpretation（解读框架）
     A1: Cases from book（书中案例）
     A2: Future triggers（未来触发场景）
     B: Boundary conditions（边界盲点）
  → 输出：每个 skill 的 SKILL.md + test-prompts.json

Stage 3: Zettelkasten 链接
  → 推理：新 skill 与已有 skill 的关系链接
  → 输出：INDEX.md（技能索引 + 关系图谱）

Stage 4: 压力测试（Darwin 兼容）
  → 推理：用 test-prompts.json 实际跑一遍，评估输出质量
  → 决策：保留 / 回炉重造 / 淘汰
  → 输出：测试结果 + 优化建议
```

### 4.3 Darwin Skill：自动化优化循环（自我进化推理链）

**推理链**：评估 → 改进 → 实测验证 → 决策（保留/回滚）→ 循环

```
Phase 0: 初始化
  → 确认优化范围、创建 git 分支（确保可回滚）

Phase 0.5: 测试 Prompt 设计
  → 为每个 skill 设计 2-3 个测试 prompt（覆盖核心能力边界）

Phase 1: 基线评估
  → 结构评分（静态分析 SKILL.md 的 8 维度）
  → 效果评分（子 agent 独立运行测试 prompt，评估输出质量）
  → 输出：基线分数表（总分 100）

Phase 2: 优化循环（棘轮机制）
  → 诊断：基于基线分数，识别最薄弱的维度
  → 改进：修改 SKILL.md（只改一个文件，单一可编辑资产）
  → 评估：重新跑结构评分 + 效果评分
  → 决策：
      ✓ 分数提升 → 保留（git commit）
      ✗ 分数下降 → 自动回滚（git reset --hard）
  → 循环：最多 10 轮，或达到 95 分以上停止

Phase 2.5: 探索性重写（可选，需用户同意）
  → 突破局部最优，尝试完全不同的写法
  → 必须用户明确批准才能执行

Phase 3: 汇总报告
  → 生成分数变化表
  → 生成成果卡片（HTML，3 种风格可选）
  → 截图保存（screenshot.mjs）
```

**五条核心原则**（确保推理链不偏离）：
1. 单一可编辑资产 — 每次只改一个 SKILL.md
2. 双重评估 — 结构评分（静态）+ 效果验证（动态）
3. 棘轮机制 — 只保留改进，自动回滚退步
4. 独立评分 — 效果维度用子 agent 评估（避免自吹自擂）
5. 人在回路 — 每个 skill 优化完后暂停等用户确认

---

## 五、多 Agent 协作机制

本工作空间通过 **技能系统（Skill System）** 和 **子 Agent 并行执行** 实现多 Agent 协作，而非依赖单一的超级 Agent。

### 5.1 技能系统：专业化 Agent 分工

每个 Skill 本质上是一个 **专业 Agent 的行为定义**，主 Agent（对话 Agent）根据任务类型调用对应的 Skill，实现"多专家协作"。

| Skill | 角色定位 | 核心能力 | 协作方式 |
|-------|---------|---------|---------|
| **hv-analysis** | 行业研究员 | 横纵分析法，产出 16000-40000 字深度报告 | 调用时启动 3 个子 Agent 并行收集信息 |
| **prompt-flow** | 提示词工程师 | 决策任务类别，生成高质量提示词 | **全局前置触发**，每次对话第一步执行 |
| **oral-copy** | 文案创作专家 | 4 类文案（数据活人感/人生感悟/产品种草/公众号长文） | 与 prompt-flow 协同：结构框架 + 叙事语言 |
| **khazix-writer** | 公众号写作风格导师 | 卡兹克写作风格库 + 5 种文章原型 | 被 oral-copy 的 D 类任务引用 |
| **book2skill** | 知识蒸馏工程师 | RIA-TV++ 六阶段流水线，将书转化为 skill | 阶段之间暂停，等待用户确认后再继续 |
| **darwin-skill** | 技能优化工程师 | 8 维度评估 + 棘轮优化循环 | 自动化执行，每个 skill 优化完暂停等人类确认 |
| **neat-freak** | 知识同步管家 | 五步同步流程，会话结束时审查所有文档 | 按需触发，通常在会话收尾时执行 |

### 5.2 并行子 Agent 协作（以 hv-analysis 为例）

```
用户：研究一下泡泡玛特

主 Agent（协调者）
  │
  ├── 加载 hv-analysis SKILL.md
  │
  ├── 启动 子 Agent 1（纵向信息收集）
  │     └── 搜索：泡泡玛特发展历程、关键转折点、创始人故事
  │
  ├── 启动 子 Agent 2（横向信息收集）
  │     └── 搜索：潮玩行业竞品（52TOYS、寻找独角兽、topjoy...）
  │
  ├── 启动 子 Agent 3（补充信息收集）
  │     └── 搜索：用户需求趋势、行业分析、消费心理
  │
  ├── [并行等待，所有子 Agent 完成后]
  │
  ├── 主 Agent 整合三方信息
  │     ├── 纵向分析（基于子 Agent 1 的数据）
  │     ├── 横向分析（基于子 Agent 2 的数据）
  │     └── 横纵交汇（综合 1+2+3，产出洞察）
  │
  └── 生成 Markdown 报告 → 转 PDF → 保存
```

### 5.3 全局前置协作：prompt-flow 的调度角色

**prompt-flow** 被设计为 **全局前置触发**，每次用户发送消息时首先执行，相当于一个"任务路由 Agent"：

```
用户消息到达
  │
  ▼
[prompt-flow 触发] ← 所有对话的入口
  │
  ├── Step 1: 反向沟通（向用户收集素材需求、使用场景）
  │
  ├── Step 2: 类别判断（匹配任务类别）
  │     ├── A 信息查找 → 调用搜索工具
  │     ├── B 头脑风暴 → 多方案生成
  │     ├── C 写作     → 调用 oral-copy 或 khazix-writer
  │     ├── D 批评     → 多维度评估
  │     ├── E 多模态   → 调用多模态生成
  │     ├── F 代码     → 调用代码工具
  │     ├── G 推理     → 启动长链推理
  │     └── E5 AI视频/图像 → 调用 prompt-flow 的 E5 子流程
  │
  ├── Step 3: 生成回答/提示词
  │
  ├── Step 4: 自检概述（上下文充分性、任务明确性、方法论匹配度）
  │
  └── 将结构化结果交给后续 Skill 执行
```

### 5.4 人在回路（Human-in-the-Loop）协作

所有长链推理流程都设计了 **人工审核节点**，确保 AI 不会偏离用户真实需求：

| 流程 | 人工审核节点 | 审核内容 |
|------|--------------|---------|
| book2skill | Stage 0 完成后、Stage 2 开始前 | 确认 BOOK_OVERVIEW.md 是否准确 |
| book2skill | Stage 2 完成后（所有 SKILL.md 已生成） | 审核每个 skill 的内容和质量 |
| darwin-skill | 每个 skill 优化完成后（Phase 2 每轮结束） | 确认是否保留改进版本 |
| darwin-skill | Phase 2.5 探索性重写前 | 必须用户明确批准才能执行 |
| hv-analysis | 报告生成后 | 可选：用户审核报告，提出修改意见 |
| neat-freak | 五步流程的"实际修改"步骤前 | 展示变更影响矩阵，用户确认后再执行 |

---

## 六、具体成果展示

### 6.1 Wiki 知识库现状（截至 2026-05-13）

| 指标 | 数值 |
|------|------|
| **总页面数** | 97 |
| **来源摘要页面** | 96（daily-feed 15 + raw 79 + book 1 + books 1） |
| **实体页面** | 0（待从来源摘要中批量提取） |
| **概念页面** | 0（待从来源摘要中批量提取） |
| **产品研究报告** | 按需生成（hv-analysis 技能就绪） |
| **演示文稿** | 按需生成（Marp 格式） |

**来源分布**：
- daily-feed：15 个（AI 视频/图像生成平台使用手册，即梦/可灵等）
- raw：79 个（品牌营销/用户研究/商业模式/AI技术/营销趋势等 8 大类）
- book：1 个（毛泽东选集 1-7 卷）
- books：1 个（已从毛选蒸馏出 10 个核心技能）

### 6.2 技能生态（7 个已安装技能）

| 技能 | 版本 | 核心方法论 | 状态 |
|------|------|-----------|------|
| hv-analysis | v2.0 | 横纵分析法（纵向+横向+交汇） | ✅ 已安装 |
| neat-freak | v1.0 | 五步同步流程 + 变更影响矩阵 | ✅ 已安装 |
| prompt-flow | v1.2 | 四阶段决策 + 7 大提示词类别 + E5 视频/图像生成 | ✅ 已安装（全局前置） |
| oral-copy | v2.0 | 4 类文案公式 + HKR 选题法 + 四层自检 | ✅ 已安装 |
| khazix-writer | v1.0 | 5 种文章原型 + 风格内核 + 19 类风格示例 | ✅ 已安装 |
| book2skill | v1.0 | RIA-TV++ 六阶段蒸馏流水线 | ✅ 已安装 |
| darwin-skill | v1.0 | 8 维度评估 + 棘轮优化循环 | ✅ 已安装 |

### 6.3 概念产品孵化成果

| 产品 | 状态 | 产出物 |
|------|------|---------|
| **PetRide「宠行」** | 概念设计阶段 | 产品定义文档 + AI 生成渲染图（混元生图 3.0） |
| **智能土豆种植器** | hv-analysis v1.0 测试 | 横纵分析报告（Markdown + PDF） |
| **无水生菜种植机** | hv-analysis v2.0 完整测试 | 横纵分析报告含商业化六维度（Markdown + PDF） |

### 6.4 自动化与定时任务

| 任务 | 状态 | 说明 |
|------|------|------|
| AI HOT 日报获取 | 可配置 | 通过 automation_update 工具配置定时获取 |
| 研报平台监控 | 可配置 | 定时抓取指定网站研究报告 |
| Wiki 健康检查 | 按需执行 | neat-freak 技能，会话结束时触发 |

---

## 七、技术实现要点

### 7.1 环境配置

| 项目 | 配置 |
|------|------|
| **操作系统** | Windows 11 |
| **Python** | 3.14.4（系统默认） |
| **Node.js** | 22.22.1 |
| **PDF 生成** | fpdf2（Windows 无 GTK，不可用 WeasyPrint） |
| **中文字体** | C:/Windows/Fonts/msyh.ttc（微软雅黑） |
| **Obsidian 集成** | Wikilink + Frontmatter + Marp 演示 |

### 7.2 MCP 工具连接状态

| 工具 | 状态 | 用途 |
|------|------|------|
| TAPD | ✅ 已连接 | 项目管理 |
| 腾讯文档 | ✅ 已连接 | 文档协作（空间 ID: XHIZlYdltTBU） |
| 腾讯乐享 | ❌ 未连接 | — |
| 微信公众号 | ❌ 未连接 | — |
| GitHub | ❌ 未连接 | 代码管理（SPLLM 仓库为 Private） |

### 7.3 四条技能构建铁律（核心约束）

> 以下四条铁律是技能系统的最高优先级规则，所有技能的创建、扩展、维护都必须遵守。

**铁律一：禁止一次性工作**
- 同一件事做 2 次以上 → 必须写成 Skill
- 周期性任务 → 加入 automation_update 定时执行

**铁律二：MECE 原则（互斥且穷尽）**
- 两个技能不能做同一件事（互斥）
- 每类工作都有对应的技能覆盖（穷尽）
- 创建新技能前，必须先列出所有现有技能，说明为什么无法扩展

**铁律三：失败判定**
- 如果用户第 2 次说同一件事 → Skill 构建失败，必须反思

**铁律四：技能构建标准流程（6 步法）**
```
Step 1: Concept    → 描述流程（自然语言）
Step 2: Prototype  → 用真实数据跑 3-10 个样本（不写技能文件）
Step 3: Evaluate   → 给用户看结果，迭代修改直到满意
Step 4: Codify     → 写成 SKILL.md
Step 5: Cron       → 加入定时任务（如果是 recurring）
Step 6: Monitor    → 监控前几次运行，继续优化
```

---

## 八、总结与展望

### 8.1 核心成果总结

| 维度 | 成果 |
|------|------|
| **知识管理** | 四层架构 Wiki，96 个来源摘要，信息可追溯 |
| **方法论体系** | 7 个技能覆盖分析/写作/蒸馏/优化全流程 |
| **长链推理** | hv-analysis 三段式（16000+ 字）、book2skill 六阶段、darwin 优化循环 |
| **多 Agent 协作** | 主 Agent 协调 + 专业化 Skill Agent + 并行子 Agent |
| **概念孵化** | PetRide 等产品从概念到渲染图的全流程支持 |
| **自动化** | 技能强制固化 + 定时任务 + neat-freak 同步 |

### 8.2 下一步规划

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P0 | 从 96 个来源摘要中批量提取 entity/concept 页面 | Wiki 知识图谱补全 |
| P0 | PetRide「宠行」概念车：完成多风格 AI 渲染图 | 支持众筹/融资材料准备 |
| P1 | 配置 AI HOT 日报自动化定时任务 | 每日自动获取 AI 行业动态 |
| P1 | 使用 darwin-skill 对所有 7 个技能进行质量优化 | 提升技能输出稳定性 |
| P2 | 将 KS2026 护生短视频自动化工作流文档化 | 固化为 Skill |
| P2 | 研报平台（发现报告/199it/萝卜投研）监控自动化 | 定时抓取+摘要+Wiki 摄取 |

---

## 附录：工作流示意图

### A. 完整 Agent 协作架构

```
用户
  │
  ▼
[prompt-flow] ← 全局前置，任务路由
  │
  ├── A 信息查找  → WebSearch / WebFetch
  ├── B 头脑风暴  → 主 Agent 多方案生成
  ├── C 写作      → [oral-copy] 或 [khazix-writer]
  ├── D 批评      → 主 Agent 多维度评估
  ├── E 多模态    → [多模态内容生成 Skill]
  ├── F 代码      → 主 Agent + Bash/PowerShell
  ├── G 推理      → [hv-analysis]（启动 3 个子 Agent 并行）
  └── E5 AI视频   → prompt-flow E5 子流程
  │
  ▼
[输出产物]
  ├── Wiki 页面（source/entity/concept/product）
  ├── Markdown 文档 / Marp 演示文稿
  ├── PDF 研究报告（hv-analysis）
  ├── 技能文件（book2skill / darwin-skill）
  └── 概念产品渲染图（多模态生成）
  │
  ▼
[neat-freak] ← 会话结束前触发，同步所有文档和记忆
```

### B. 信息处理全链路

```
原始信息（Layer 1）
  │  daily-feed/  → AI 平台使用手册
  │  raw/        → 品牌营销/用户研究/商业模式...
  │  book/       → 精品书籍
  │  books/      → 已蒸馏的技能化书籍
  ▼
[Ingest 工作流]
  │  阅读 → 提取实体/概念/数据 → 创建 source 摘要
  ▼
来源摘要（Layer 2）
  │  wiki/sources/YYYY-MM-DD-标题.md
  │  包含：核心观点、关键数据、洞察、相关实体/概念
  ▼
[Query / Digest / Produce 工作流]
  │  跨页面整合 → Wikilink 关联 → 深度分析
  ▼
结构化知识（Layer 3）
  │  entities/  → 公司/产品/人物
  │  concepts/  → 技术/趋势/方法论
  │  products/  → 概念产品研究报告
  │  presentations/ → Marp 演示文稿
  ▼
[Check 工作流]
  │  内容一致性 / 结构完整性 / 链接有效性
  ▼
规范约束（Layer 4）
  │  SCHEMA.md  → 完整规范文档
  │  CLAUDE.md  → Agent 操作手册
  │  AGENTS.md   → 系统说明书
  └── skills/    → 7 个专业化 Agent 行为定义
```

---

*本文档由 LLM Agent 基于工作空间实际文件（CLAUDE.md / AGENTS.md / index.md / log.md）自动生成。*
